import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
 appId: 'com.yourname.upiannouncer',
 appName: 'UPI Announcer',
 webDir: 'dist',
 bundledWebRuntime: false,
 plugins: {
  TextToSpeech: {
   displayName: 'Text to Speech',
   description: 'Announce UPI transactions'
  },
  Notification: {
   displayName: 'Notifications',
   description: 'Listen for UPI transaction notifications'
  }
 },
 server: {
  androidScheme: 'https',
  cleartext: true,
  allowNavigation: ['*']
 },
 android: {
  allowMixedContent: true,
  captureInput: true,
  // Fixed: Use a boolean literal instead of process.env
  webContentsDebuggingEnabled: true // Enable for development
 }
};

export default config;